'use strict';

var app = angular.module('HibiApp',
  [ 'angular.markdown'
  , 'ui.sortable'
  , 'ng.group'
  , 'app.models'
  , 'app.services'
  , 'app.controllers'
  , 'app.directives'
  , 'app.filters'
  ])
.value('defaultTaskSchedule', #{toJSON Once})
.value('initialPlanData', #{toJSON activePlans})
.value('initialTaskData', #{toJSON tasksWithChildren});


app.filter('scheduleLabel', function () {
  var labels = {
    #{toJSON Once}: '',
    #{toJSON Daily}: '♳',
    #{toJSON Weekly}: '♹',
    #{toJSON Fortnightly}: '♹♹'
  };
  return function (schedule) {
    return labels[schedule];
  };
});
