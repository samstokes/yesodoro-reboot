<span ##{widgetId} .expandy-indicator> 
  #{expandyIndicator initialState}
  <script language=javascript>
    \$(function () {
      expandy('#{widgetId}', '#{handleSelector}', '#{targetSelector}');
    });
